- hosts: medias-servers
  user: little_alice
  sudo: true

  tasks:
    - name : Stop Emby Server
      service:
        name=emby-server
        state=stopped

    - name : Start Emby Server
      service:
        name=emby-server
        state=started